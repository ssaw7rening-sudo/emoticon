# 웹 도구 완성도 및 구글 애드센스 승인 최적화 계획

메인 사이트는 100% 원페이지 웹앱의 빠른 사용성을 유지하면서, 생성기 하단에 6개 전문 정보 섹션을 추가하고 `/privacy` 및 `/terms` 독립 URL을 구성하여 구글 애드센스 심사 통과와 사용자 만족도를 극대화한다.

1. **독립 URL 라우팅 및 약관 페이지 구성 (`/privacy`, `/terms`)**:
   - `window.location.pathname` 및 HTML5 History API 기반 클라이언트 라우팅 연동.
   - `vercel.json` SPA 리라이트 설정으로 직접 URL 접근 시 404 없이 즉시 로드.
   - 애드센스/GDPR 규정 준수 개인정보처리방침(Google AdSense 쿠키, 웹로그, 수집 항목, 문의처) 및 이용약관 작성.
   - 푸터에서 모달 또는 독립 페이지로 즉시 열람 및 복귀 기능 제공.

2. **메인 생성기 하단 6개 전문 정보 섹션 구축**:
   - 섹션 1: `이모티콘 제작이 처음인가요? (About AI Prompt Maker)`
   - 섹션 2: `3분 초간단 사용 가이드 (Step-by-Step Tutorial)`
   - 섹션 3: `완성도를 높이는 이모티콘 기획 5대 원칙 (Pro Tips)`
   - 섹션 4: `추천 이모티콘 15종 필수 감정 황금 조합 (15 Core Emotion Formula)`
   - 섹션 5: `사진 & 반려동물로 나만의 캐릭터 만들기 (Photo Caricature Guide)`
   - 섹션 6: `자주 묻는 질문 아코디언 (FAQ & Studio Guidelines)`

3. **린트, 프로덕션 빌드, Capacitor Android 동기화, GitHub 배포 검증**.

## 성공 기준

- 390px 화면에서 가로 넘침과 단어 중간 줄바꿈이 없다.
- 주요 실행 버튼의 높이가 모바일에서도 64px로 유지된다.
- 작은 강조 문구가 충분히 어두운 색으로 표시된다.
- 데스크톱의 3열 문구 그리드와 2열 실행 버튼이 유지된다.
- 린트와 빌드가 통과하고 브라우저 콘솔 오류가 없다.

## 후속 색상 개선

붉은 버튼 계열을 민트 팔레트로 교체한다. 문구 입력과 캐릭터 태그는 연한 민트, 선택 상태와 랜덤 버튼은 진한 민트를 사용하고 크림색 카드와 따뜻한 히어로는 유지한다.

## 후속 사용성 개선

클릭·입력 가능한 요소에만 호버 피드백을 추가하고, 소개 문구를 출시 목적이 아닌 AI 프롬프트 제작 도구의 역할에 맞게 고친다. 영문 카테고리 메뉴는 여러 줄로 배치해 잘림을 제거한다.

## 후속 세부 조정

초기화와 테마 선택에서는 떠오르는 호버를## 피사체 카테고리 확장 및 이모티콘 테마 정비 계획

피사체 생태계(새, 어패류, 곤충, 파충류, 공룡, 인형/팬시)와 신규 문구 테마를 확장하고, 카테고리 간 중복을 완전 제거하며 직관적인 순서로 재배치한다.

1. **태그 카테고리 확장 및 중복 제거**:
   - `🐱 동물`에서 조류·해양생물 태그를 분리하여 신규 카테고리로 이전한다.
   - 신규 피사체 카테고리 추가: `🐦 새/조류`, `🐟 어패류/해양생물`, `🐞 곤충/벌레`, `🦎 파충류/양서류`, `🦖 공룡/고생물`, `🧸 인형/문구/팬시`.
   - 신규 화풍 추가: `B급 병맛/코믹 짤툰풍`, `크레파스/오일파스텔 동화풍`, `동양화/수묵담채화풍`.
   - 카테고리 탭 순서: [피사체 11종] → [외형/성격/화풍 3종] → [의상/소품 2종] → [상황/테마 6종] → [배경/효과 1종] 순으로 정렬.

2. **15종 문구 테마(THEMES) 8개 신규 세트 추가**:
   - `새/반려조 ①, ②`, `수족관/해양생물 ①, ②`, `숲속/곤충 ①, ②`, `파충류/우파루파 ①, ②`, `군인/곰신 ①, ②`, `K-직장인 속마음(매운맛) ①, ②`, `집사/고양이 전용 ①, ②`, `댕댕이/강아지 전용 ①, ②`.

3. **4개 국어(KO, EN, JA, ZH) 완벽 동기화**:
   - 모든 신규 테마 및 캐릭터 태그를 4개 국어로 번역/연동.

4. **단일 선택/다중 선택 및 랜덤 생성 로직 동기화**:
   - `koSubjectKeys`, `enSubjectKeys`, `allSubjects` 등에 신규 피사체 카테고리 완벽 매핑.

5. **린트 및 프로덕션 빌드, 안드로이드 Capacitor 동기화 검증**.
## 생성 모드 복원과 노란 버튼 복귀

1. 남아 있는 생성 상태와 프롬프트 분기 로직에 세 가지 선택 UI를 다시 연결한다.
2. `15종 개별 분할`에서는 선택한 AI 모델의 프롬프트를 문구별로 복사할 수 있게 한다.
3. 하단 복사 버튼은 동일한 노란색 계열로 되돌린다.
4. 각 모드의 미리보기와 복사 동작을 브라우저에서 검증한다.
## 생성 문구 선택 방식과 색상 완화

1. `1종`의 자유 입력을 제거하고 현재 문구 15개 중 하나를 고르는 선택 메뉴로 바꾼다.
2. `15종 개별`은 문구별 선택 버튼 구조를 유지한다.
3. 생성 모드와 복사 버튼의 노란색을 채도가 낮은 크림 골드로 완화한다.
4. 선택값과 프롬프트 연동, 모바일 레이아웃을 검증한다.
## 15종 개별 미리보기와 공통 복사 버튼 수정

1. `15종 개별`의 문구 버튼을 복사 버튼이 아니라 미리보기 대상 선택 버튼으로 바꾼다.
2. 선택한 문구를 GPT와 Gemini 프롬프트 생성에 동일하게 연결한다.
3. GPT와 Gemini 복사 버튼을 세 모드 모두에서 항상 표시한다.
4. 선택 문구 반영과 실제 복사 내용을 브라우저에서 검증한다.
## 선택 문구 미리보기 가시성 수정

1. 선택 문구가 실제로 바뀌는 위치를 브라우저에서 비교한다.
2. 단일 이미지 프롬프트의 첫 부분에 선택 문구를 배치한다.
3. 미리보기 상단에 현재 선택 문구를 별도 표시한다.
4. GPT와 Gemini 양쪽에서 첫 화면 변경을 검증한다.
## 15종 개별 원클릭 복사

1. 15종 개별 문구 버튼에 현재 선택 AI 기준의 복사 동작을 연결한다.
2. 문구 선택, 미리보기 갱신, 복사 완료 표시를 한 번의 클릭으로 처리한다.
3. 하단 GPT·Gemini 공통 복사 버튼은 유지한다.
4. GPT와 Gemini 각각의 원클릭 복사 흐름을 검증한다.
## 생성 모드 위치 이동과 자유 입력 복원

1. 생성 모드 영역을 문구 그리드 아래에서 사진 팁과 프롬프트 미리보기 사이로 옮긴다.
2. 자유 개별의 문구 선택 메뉴를 직접 입력 필드로 변경한다.
3. 직접 입력한 임의 문구가 GPT와 Gemini 프롬프트에 반영되도록 분기를 수정한다.
4. 15종 개별 원클릭 복사 동작은 유지한다.
## 사진 활용 팁 위치 이동

1. 프롬프트 미리보기 상단의 사진 활용 팁 카드를 제거한다.
2. 복사 버튼 다음, 활용 가이드 바로 앞에 같은 카드를 배치한다.
3. 카드 내용과 반응형 디자인이 유지되는지 검증한다.
## 캐릭터 화풍 메뉴 추가

1. 캐릭터 설정 카테고리에 한글 `화풍`, 영문 `Art Style` 메뉴를 추가한다.
2. 이모티콘에 적합한 2D 중심 화풍 선택지를 제공한다.
3. 선택한 화풍이 캐릭터 설명과 GPT·Gemini 프롬프트에 반영되는지 검증한다.
4. 한글 가로 스크롤과 영문 줄바꿈 메뉴 레이아웃을 확인한다.
## 캐릭터 카테고리 재정리와 두 줄 메뉴

1. 현재 태그를 역할별로 검토해 잘못 분류된 의상과 화풍 항목을 이동한다.
2. 캐릭터 일관성에 필요한 외형/특징 카테고리를 한글과 영문에 추가한다.
3. 의상과 소품 카테고리 명칭을 명확하게 다듬는다.
4. 한글과 영문 카테고리 메뉴 모두 줄바꿈 레이아웃을 사용한다.
5. 태그 선택과 프롬프트 반영, 데스크톱 두 줄과 모바일 배치를 검증한다.

## 생성 프롬프트 충돌과 입력 검증 개선

1. 사진이 없는 기본 상태에서 첨부 사진을 전제로 하지 않도록 캐릭터 컨텍스트를 수정한다.
2. 사용자가 고른 화풍이 고정 화풍 문장보다 우선하도록 GPT·Gemini 프롬프트를 조정한다.
3. 출시 목적처럼 보이는 상업용·플랫폼 종속 표현을 개인용 메신저 스티커 표현으로 변경한다.
4. 개별 프롬프트의 이전 시트 참조를 조건부 문장으로 변경한다.
5. 빈 문구와 중복 문구를 복사 전에 검증하고 한글·영문 안내를 표시한다.
6. 여섯 생성 조합과 린트·빌드·브라우저 동작을 검증한다.

## 사진 참고 캐릭터 설정

1. 캐릭터 설정 상단에 직접 설정과 사진 참고 선택 메뉴를 추가한다.
2. 사진 참고 모드에 닮게, 특징 반영, 캐릭터화의 세 반영 방식을 제공한다.
3. 사진은 사이트에 업로드하지 않고 AI 채팅창에 함께 첨부하도록 명확히 안내한다.
4. 선택한 사진 반영 방식을 GPT·Gemini의 시트와 개별 프롬프트에 반영한다.
5. 한글·영문과 데스크톱·모바일 화면 및 프롬프트 복사를 검증한다.

## Gemini 프롬프트 구조 고도화

1. 선택 태그를 대상, 외형, 성격, 의상, 화풍, 소품, 효과 역할로 다시 분류해 Gemini 프롬프트에 전달한다.
2. Gemini 시트와 개별 프롬프트를 목표, 참고 이미지, 고정 정체성, 화풍, 장면, 구도, 일관성, 제외 조건 순으로 재구성한다.
3. 시트에는 5열 3행과 셀 경계 조건을, 개별에는 1대1 정사각형과 여백 조건을 명시한다.
4. Gemini에 글자 없음과 정확한 문구 포함 선택 기능을 추가한다.
5. 시트는 초안, 15종 개별 분할은 최종 결과용이라는 안내를 추가한다.
6. Gemini와 Imagen 3을 동일시하는 화면 문구를 현재 기능 중심 표현으로 변경한다.
7. 직접 설정과 사진 참고, 세 생성 모드, 한글·영문, 모바일, 린트와 빌드를 검증한다.

## GPT 이미지 프롬프트 구조 고도화

1. 기존 태그를 대상, 외형, 성격, 의상, 화풍, 소품, 효과로 분리해 GPT 프롬프트에 전달한다.
2. GPT 시트와 개별 프롬프트를 목표, 참고 이미지, 고정 정보, 화풍, 장면, 구도, 일관성, 글자 정책, 제외 조건 순으로 재구성한다.
3. 시트의 15개 문구를 3행 5열의 정확한 셀 위치와 연결한다.
4. GPT에 문구 포함과 글자 없음 선택 기능을 추가한다.
5. 투명 배경, 단색 배경, 크로마키 배경 선택 기능을 추가한다.
6. 화면의 DALL·E 3 고정 표기를 ChatGPT 이미지 생성 표현으로 변경한다.
7. GPT와 Gemini 설정 분리, 세 생성 모드, 한글·영문, 모바일, 복사, 린트와 빌드를 검증한다.
## Gemini 결과물 품질 개선

1. Gemini 전용 작업 단계를 기준 캐릭터 생성과 최종 개별 생성으로 분리한다.
2. 최종 생성 프롬프트는 첨부한 기준 캐릭터 이미지를 변경 금지 기준으로 사용한다.
3. 캐릭터 변화, 신체 잘림, 문구 오류를 수정하는 후속 프롬프트 복사 기능을 제공한다.
4. 기존 GPT 흐름과 Gemini 15종 초안 기능을 유지한 채 린트, 빌드, 반응형 화면을 검증한다.
