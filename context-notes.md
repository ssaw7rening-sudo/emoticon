# 작업 컨텍스트 노트

## 2026-08-09

- GPT 고도화 1차 범위는 생성 프롬프트의 구조, 패널 매핑, 글자 여부, 배경 방식, 모델 표기 정리로 제한한다.
- 결과 이미지의 일부만 다시 고치는 수정 전용 프롬프트는 생성 UI와 역할이 다르므로 별도 단계로 남긴다.
- GPT는 문구 포함을 기본값으로 유지하고, 배경은 후처리가 적은 투명 배경을 기본값으로 사용한다.
- 브라우저 검증에서 대상, 외형, 성격, 의상, 화풍, 소품, 효과가 GPT 프롬프트의 각 역할 줄에 분리되어 표시됐다.
- GPT 시트에는 9개 역할 블록과 1행 1열부터 3행 5열까지 정확한 15개 문구 위치가 포함됐다.
- GPT 개별에는 1대1 정사각형, 사방 12% 여백, 문구 한 번만 표시와 변경 허용 요소가 포함됐다.
- 문구 포함과 글자 없음, 투명·단색·크로마키의 모든 선택이 프롬프트 정책을 즉시 변경했다.
- 사진 참고의 특징만 반영 방식이 GPT 고정 정보와 참고 이미지 지시에 반영됐고 15종 개별 원클릭 복사가 완료됐다.
- Gemini 탭에서는 GPT 전용 설정이 숨겨지고 기존 Gemini 프롬프트와 설정이 유지됐다.
- 영문 390px 화면에서 GPT 설정이 모두 표시되고 DALL·E 3 고정 표기, 가로 넘침, 콘솔 오류·경고가 없었다.
- 사용되지 않던 이전 캐릭터 상태 5개와 초기화 호출을 제거했으며 `npm.cmd run lint`와 1,783개 모듈의 프로덕션 빌드가 경고 없이 통과했다.
- Gemini 프롬프트는 길이를 늘리기보다 정보 역할과 우선순위를 분리해 모델이 고정 요소와 변경 요소를 구분하도록 한다.
- 기존 태그 입력 UI는 유지하되 프롬프트 생성 시 알려진 태그를 카테고리별로 다시 인식해 구조화한다.
- Gemini의 기본값은 안정적인 글자 없음으로 유지하고, 사용자가 원할 때만 정확한 문구 포함을 선택한다.
- 15개를 한 캔버스에 정확히 배치하는 생성은 모델 편차가 있으므로 시트 전체는 초안, 개별 분할은 최종 결과용으로 안내한다.
- 브라우저 검증에서 동물, 외형, 성격, 화풍, 의상, 소품, 효과 태그가 Gemini 프롬프트의 각 역할 줄에 분리되어 표시됐다.
- Gemini 시트 프롬프트에는 9개 역할 블록, 15개 패널 문구, 5열 3행, 셀 간 침범 금지 조건이 포함됐다.
- Gemini 개별 프롬프트에는 1대1 정사각형, 사방 12% 여백, 명확한 표정 하나와 전신 자세 하나, 변경 허용 요소가 포함됐다.
- 글자 없음에서는 문구를 시각적 맥락으로만 사용하고, 문구 포함에서는 정확한 철자 확인과 한 번만 렌더링하는 조건으로 즉시 전환됐다.
- 사진 참고의 캐릭터화 방식과 참조 우선순위가 Gemini 개별 프롬프트에 반영됐고 15종 개별 버튼의 원클릭 복사가 완료됐다.
- GPT 탭에서는 Gemini 전용 글자 설정이 숨겨지고 기존 GPT 프롬프트가 유지됐다.
- 영문 390px 화면에서 Gemini 설정과 안내가 모두 표시되고 `Imagen 3` 고정 표기, 가로 넘침, 콘솔 오류·경고가 없었다.
- `npm.cmd run lint`와 1,783개 모듈의 프로덕션 빌드가 통과했다.
- 사진 파일은 프롬프트 복사와 함께 전달할 수 없고 개인정보 처리 범위도 커지므로 사이트 자체 업로드 기능은 만들지 않는다.
- 캐릭터 기준은 기본값인 직접 설정과 사용자가 AI 채팅에 사진을 함께 첨부하는 사진 참고 모드로 나눈다.
- 사진 참고 모드는 최대한 닮게, 특징만 반영, 귀엽게 캐릭터화의 세 가지 반영 강도를 제공하며 기존 화풍과 소품 태그는 함께 사용할 수 있다.
- 직접 설정 기본 화면에서는 사진 반영 방식과 참고 이미지 배지가 숨겨지고 프롬프트도 사진을 필수로 요구하지 않았다.
- 사진 참고 기본값은 귀엽게 캐릭터화이며, 세 반영 방식의 이름과 세부 지시가 GPT 시트 및 Gemini 시트·개별 프롬프트에 각각 반영됐다.
- 한글에서 선택한 특징만 반영 상태는 영문 전환 후 `Keep key features`로 유지됐다.
- 사진 참고 상태에서 GPT와 Gemini 복사 완료 아이콘이 모두 표시됐다.
- 390px 영문 화면에서 사진 메뉴의 세 방식과 안내가 모두 표시되고 가로 넘침 및 콘솔 오류·경고가 없었다.
- `npm.cmd run lint`와 1,783개 모듈의 프로덕션 빌드가 통과했다.
- 사이트 안에는 사진 업로드 입력이 없으므로 기본 캐릭터 컨텍스트에서 첨부 사진을 자동으로 가정하지 않는다. 사용자가 AI 대화창에 사진을 함께 첨부하는 경우를 위한 조건부 참고 이미지 문장은 유지한다.
- 화풍 태그를 선택하면 기존의 벡터·파스텔 고정 지시 대신 선택 화풍을 최우선으로 사용한다. 화풍을 고르지 않았을 때만 기본 2D 이모티콘 스타일을 제공한다.
- 이 유틸리티는 출시를 직접 목적으로 하지 않으므로 프롬프트의 상업용·카카오톡 샵 순위 표현을 개인용 고품질 메신저 스티커 표현으로 바꾼다.
- 빈 문구나 중복 문구를 임의로 대체하지 않고 복사를 막은 뒤 사용자가 직접 고치도록 안내한다.
- 기본 GPT 시트 프롬프트에서 잘못된 첨부 사진 전제가 사라지고, 사진을 실제로 함께 붙이는 경우를 위한 조건부 참고 문장만 남았다.
- 수채화 다음 픽셀 화풍을 선택했을 때 캐릭터 입력과 GPT·Gemini 프롬프트에는 마지막 픽셀 화풍 하나만 남았으며 벡터 고정 지시는 포함되지 않았다.
- GPT·Gemini의 시트 전체·자유 개별·15종 개별 분할 여섯 조합에서 각각 15장 또는 1장 조건과 선택 문구가 올바르게 표시됐다.
- 빈 문구와 중복 문구에서 한글 안내 및 두 복사 버튼 비활성화를 확인했고, 정상 입력 복원 후 GPT 복사 완료 아이콘이 표시됐다.
- 390px 영문 화면에서 가로 넘침과 콘솔 오류·경고가 없었으며 `npm.cmd run lint`와 1,783개 모듈의 프로덕션 빌드가 통과했다.
- 캐릭터의 종과 성격만으로는 생성 이미지의 외형 일관성을 구체화하기 어려워 `외형/특징`과 `Appearance` 카테고리를 추가한다.
- 화풍과 혼동되는 `의상/스타일`은 `의상`으로 명확히 하고, 소품 카테고리 안의 의사 가운·요리사 앞치마·회사원 정장을 의상으로 이동한다.
- 배경/효과에 있던 파스텔톤·수채화·도트 표현은 기존 화풍 선택지와 중복되므로 제거한다.
- 한글과 영문 카테고리 메뉴 모두 가로 스크롤 대신 줄바꿈을 사용해 전체 분류를 한눈에 확인할 수 있게 한다.
- 820px 브라우저에서 한글과 영문 9개 카테고리가 각각 정확히 2줄로 배치됐다.
- `크고 반짝이는 눈` 선택값은 캐릭터 설명과 GPT·Gemini 프롬프트 미리보기에 모두 반영됐다.
- 의사 가운은 의상에만 표시되고 소품/동작에는 표시되지 않으며, 배경/효과의 화풍 중복 3개도 제거됐다.
- 390px 영문 화면에서 9개 카테고리가 모두 표시되고 문서 가로 넘침과 브라우저 콘솔 오류·경고가 없었다.
- `npm.cmd run lint`와 1,783개 모듈의 프로덕션 빌드가 통과했다.
- 전체 파스텔 분위기와 Paperlogy 폰트는 유지한다.
- `#FFD166`과 `#FFB7B2`는 채움색으로는 적합하지만 흰색이나 크림색 위의 작은 글자색으로는 대비가 부족하다.
- 강조 텍스트에는 어두운 금색과 어두운 장밋빛 색상을 별도 토큰으로 사용한다.
- 모바일에서 `flex-1 h-[64px]` 버튼은 세로 Flex의 주축 크기 계산 때문에 실제 높이가 24px로 축소된다. `min-h-[64px]`과 반응형 Flex 속성으로 해결한다.
- 광고는 실제 운영 ID가 아직 없으므로 제거하지 않고 모바일 플레이스홀더 높이만 제한한다.
- Git 저장소가 아니므로 의미 단위 커밋은 만들 수 없다.
- 구현 중 임시 광고 ID가 사용되는 동안에는 광고 컴포넌트를 렌더링하지 않는 방식이 빈 공간을 가장 확실히 방지한다고 판단했다. 실제 ID로 교체하면 기존 광고 렌더링이 자동으로 활성화된다.
- 밝은 채움색은 유지하고 `primary-strong`, `secondary-strong` 토큰을 추가해 글자와 포커스 상태에만 적용했다.
- 정적인 카드가 클릭 가능한 요소처럼 움직이지 않도록 `shadow-bubbly`의 전역 호버 이동을 제거하고 그림자만 유지했다.
- `npm.cmd run lint`는 오류 없이 통과했으며 기존 비활성 생성 모드의 미사용 setter 경고 2건이 남아 있다.
- 별도 임시 경로에서 프로덕션 빌드가 통과했으며 검증 산출물은 제거했다.
- 최종 브라우저 검증에서 데스크톱 복사 버튼은 64px, 모바일 복사 버튼도 64px로 측정됐다.
- 390px 모바일에서 문서 너비와 스크롤 너비가 모두 375px로 측정되어 가로 넘침이 없었다.
- 마지막 문구 입력란의 중심과 화면 중심이 모두 188px로 측정되어 중앙 정렬을 확인했다.
- 미설정 광고의 보이는 카드 수는 0개였고 브라우저 콘솔 오류와 경고도 없었다.
- 모바일 히어로 장식은 작은 화면에서 제목과 겹치지 않도록 숨기고 데스크톱에서만 유지했다.
- 사용자가 붉은 버튼 계열보다 크림 또는 민트를 선호해 크림 배경은 유지하고 버튼만 민트로 바꾸기로 했다.
- 민트 팔레트는 `#CFF3E5`, `#AEE8D5`, `#76CDB3`, `#184F43`, `#B8E4D5`를 사용한다.
- 민트 글자색 `#184F43`은 연한 민트에서 7.87:1, 선택 민트에서 4.98:1의 대비를 확보한다.
- 기존 노랑 기본 강조색은 히어로와 브랜드 보조색으로 유지하고, 조작 요소의 포커스와 선택 상태만 민트로 통일했다.
- 정적 카드의 그림자도 분홍색 대신 낮은 불투명도의 민트 그림자로 변경했다.
- 모바일 실측에서 문구 입력은 연한 민트 `rgb(207, 243, 229)`, 선택 탭과 랜덤 버튼은 진한 민트 `rgb(118, 205, 179)`로 확인됐다.
- 390px 화면에서 가로 넘침이 없고 브라우저 콘솔 오류와 경고도 없었다.
- 사용자가 첨부한 영문 화면에서 긴 카테고리명이 가로 스크롤 영역 안에서 잘려 보이는 문제를 확인했다.
- 서비스의 핵심 목적은 이모티콘 출시 지원이 아니라 ChatGPT와 Gemini용 이모티콘 제작 프롬프트를 쉽게 조합하는 것이다.
- 호버는 정적 카드가 아니라 실제 버튼과 입력 요소에만 적용한다.
- 호버 가능 마우스 환경에서만 2px 상승과 민트 그림자를 적용하고, 키보드·터치 활성 상태에는 0.98 배율 피드백을 사용한다.
- 모션 감소 설정에서는 호버 전환 시간을 사실상 제거한다.
- 영문 카테고리 바만 `flex-wrap`을 사용하고 한글 모드는 기존 가로 스크롤 방식을 유지한다.
- 브라우저 실측에서 호버 전에는 transform이 없고 호버 중에는 `translateY(-2px)`과 민트 그림자가 적용됐다.
- 영문 메뉴는 820px 화면에서 2줄, 390px 화면에서 4줄로 배치되며 모든 항목이 컨테이너 안에 들어왔다.
- 영문 모바일에서 문서 너비와 스크롤 너비가 모두 375px로 측정되어 가로 넘침이 없었고 콘솔 오류와 경고도 없었다.
- 사용자가 지정한 호버 제외 대상은 `초기화` 버튼과 테마 선택 드롭다운이다.
- 프롬프트 미리보기는 현재 `font-bold`라 장문의 영문에서 지나치게 무거워 보인다.
- 하단 두 복사 버튼은 기능 우선순위가 동일하므로 같은 민트 배경과 짙은 민트 글자로 통일한다.
- 초기화와 테마 선택에서 `interactive-control` 클래스를 제거해 이동과 그림자 호버가 발생하지 않게 했다.
- 프롬프트 미리보기는 `font-normal`로 변경했고 두 복사 버튼은 `bg-mint`, `text-mint-strong` 조합을 공유한다.
- `npm.cmd run lint`는 오류 없이 통과했으며, 기존 미사용 setter 경고 2건만 남았다.
- 별도 임시 출력 경로의 프로덕션 빌드는 1,783개 모듈을 처리해 통과했다.
- 820×800 브라우저에서 초기화 호버의 transform과 그림자가 없고, 미리보기 글자 굵기는 400이며, 두 복사 버튼 배경색은 `rgb(118, 205, 179)`로 동일함을 확인했다.
- 최종 브라우저 콘솔에는 오류와 경고가 없었다.
- 첨부 화면에서는 복사 버튼 배경이 페이지와 거의 같아 버튼의 기능적 중요도가 드러나지 않았다.
- 이전 요청대로 두 버튼은 같은 색을 유지하고, 설정 재시작 여부에 영향받지 않도록 현재 소스에서 즉시 생성되는 명시적 민트 색상 클래스를 사용한다.
- 브라우저에서 두 버튼 기본 배경은 `rgb(118, 205, 179)`, 테두리는 `rgb(79, 174, 146)`, 글자는 `rgb(24, 79, 67)`로 동일했다.
- ChatGPT 버튼 호버 시 배경은 `rgb(101, 196, 168)`로 짙어지고 2px 상승 효과가 적용됐다.
- 린트와 프로덕션 빌드가 통과했으며 브라우저 콘솔에는 오류와 경고가 없었다.
- 생성 모드 UI는 사라졌지만 `generationMode`, `individualPhrase`, 단일 문구용 프롬프트 분기와 다국어 문구는 소스에 남아 있었다.
- 세 모드는 한 캔버스에 15개를 만드는 `시트 전체`, 자유 문구 하나를 만드는 `자유 개별`, 현재 15개 문구를 각각 한 장용 프롬프트로 복사하는 `15종 개별 분할`로 복원한다.
- 사용자의 이전 동일 색상 요청은 유지하면서 민트 대신 두 복사 버튼 모두 노란색으로 되돌린다.
- `시트 전체`에서는 15개 그리드 프롬프트와 노란 복사 버튼 2개가 표시되고, `자유 개별`에서는 입력한 한 문구용 프롬프트가 표시된다.
- `15종 개별 분할`에서는 일반 복사 버튼 대신 선택한 AI 모델 기준의 문구별 복사 버튼 15개를 표시한다.
- 브라우저에서 첫 번째 개별 복사 버튼을 눌렀을 때 완료 아이콘으로 바뀌어 복사 동작이 연결됐음을 확인했다.
- 노란 버튼의 기본 배경은 `rgb(255, 209, 102)`, 테두리는 `rgb(232, 184, 74)`, 글자는 `rgb(74, 51, 0)`로 두 버튼이 동일했다.
- 390px 한글과 영문 화면 모두 가로 넘침이 없고, 15개의 개별 복사 버튼이 유지됐으며 브라우저 콘솔 오류와 경고가 없었다.
- 최종 `npm.cmd run lint`와 1,783개 모듈의 프로덕션 빌드가 경고 없이 통과했다.
- 사용자는 진한 노란색의 면적감이 과하다고 느꼈으므로 버튼 색상을 페이지 배경과 구분되는 부드러운 크림 골드로 낮춘다.
- `1종`은 별도 문구를 직접 타이핑하는 대신 위 문구 그리드의 15개 값 중 하나를 선택한다.
- `15종 개별`은 이미 15개 문구 버튼 중 원하는 항목을 선택해 복사하는 방식이므로 이 구조를 유지한다.
- `1종` 선택 메뉴에서 `고마워요`를 고르면 미리보기 프롬프트에도 `고마워요`가 반영되는 것을 확인했다.
- `15종 개별`에는 한글과 영문 모두 문구 선택 버튼이 15개 표시됐다.
- 크림 골드 버튼의 실제 배경색은 `rgb(255, 232, 181)`이며, 이전 노랑 `rgb(255, 209, 102)`보다 채도를 낮췄다.
- 390px 한글과 영문 화면에서 가로 넘침이 없고 브라우저 콘솔 오류와 경고도 없었다.
- `npm.cmd run lint`와 1,783개 모듈의 프로덕션 빌드가 경고 없이 통과했다.
- `15종 개별` 문구 버튼은 현재 선택된 미리보기 모델의 프롬프트를 즉시 복사하도록 변경한다.
- 문구 버튼의 직접 복사는 빠른 반복 작업용이고, 하단 GPT·Gemini 버튼은 모델을 명시해 다시 복사할 수 있는 공통 동작으로 유지한다.
- ChatGPT 활성 상태에서 5번 `고마워요`를 누르면 GPT 프롬프트가 선택·복사되고 버튼에 완료 아이콘이 표시됐다.
- Gemini 활성 상태에서 6번 `사랑해요`를 누르면 Gemini 단일 이미지 프롬프트가 선택·복사되고 완료 아이콘이 표시됐다.
- 두 경우 모두 선택 문구가 미리보기에 즉시 반영됐고 하단 GPT·Gemini 복사 버튼 2개는 유지됐다.
- 390px 화면에서 15개 직접 복사 버튼과 하단 복사 버튼 2개가 유지되고 가로 넘침이 없었다.
- 브라우저 콘솔 오류와 경고가 없고, 린트와 1,783개 모듈의 프로덕션 빌드가 경고 없이 통과했다.
- 의사 가운, 요리사 앞치마, 회사원 정장은 소품이 아니라 의상으로 이동한다.
- 파스텔톤, 수채화 느낌, 레트로 도트 효과는 화풍 선택지와 중복되므로 배경/효과에서 제거한다.
- `의상/스타일`은 화풍의 스타일과 혼동되므로 `의상`, `소품/동작`은 구분이 쉬운 가방 아이콘으로 변경한다.
- 얼굴형, 눈, 볼, 털, 귀, 꼬리, 체형 등을 모은 `외형/특징`은 캐릭터 일관성을 높이는 새 카테고리로 추가한다.
- 새 카테고리 이름은 사용자가 바로 이해할 수 있는 `🖌️ 화풍`과 `🖌️ Art Style`을 사용한다.
- 기존 프롬프트의 2D 이모티콘 방향과 충돌을 줄이기 위해 만화, 웹툰, 수채화, 벡터, 픽셀 아트 등 2D 중심 선택지를 제공한다.
- 브라우저 검증에서 영문 전환 후 한글 카테고리 키가 남아 태그 목록이 비는 기존 문제를 확인했으므로 같은 순번의 번역 카테고리로 함께 전환한다.
- 한글 화풍과 영문 Art Style에서 각각 12개 선택지가 표시됐다.
- `귀여운 2D 만화풍`과 `Cute 2D cartoon` 선택값이 캐릭터 설명 및 GPT·Gemini 프롬프트에 반영됐다.
- 화풍 탭을 선택한 상태로 언어를 바꾸면 번역된 화풍 탭과 선택지 12개가 그대로 활성화됐다.
- 390px 영문 화면에서 8개 카테고리가 줄바꿈되어 모두 표시됐고 가로 넘침이 없었다.
- 브라우저 콘솔 오류와 경고가 없고, 린트와 1,783개 모듈의 프로덕션 빌드가 경고 없이 통과했다.
- 사용자가 지정한 새 위치는 하단 GPT·Gemini 복사 버튼 다음이자 `활용 가이드` 섹션 바로 앞이다.
- 사진 활용 팁 카드의 내용과 스타일은 변경하지 않고 JSX 위치만 이동한다.
- 브라우저 문서 순서에서 프롬프트 미리보기 다음에 사진 활용 팁이 나오고, 바로 뒤에 활용 가이드가 배치됐다.
- 사진 활용 팁은 중복 없이 1개만 렌더링됐다.
- 390px 화면에서 가로 넘침 없이 카드 바로 아래에 활용 가이드가 표시됐다.
- 브라우저 콘솔 오류와 경고가 없고, 린트와 1,783개 모듈의 프로덕션 빌드가 경고 없이 통과했다.
- 생성 모드는 프롬프트 결과에 영향을 주는 조작이므로 문구 그리드 아래보다 사진 팁과 미리보기 사이가 관계를 이해하기 쉽다.
- 자유 개별은 테마 문구에 제한하지 않고 사용자가 임의 상황과 감정을 직접 입력할 수 있게 복원한다.
- 생성 모드 영역은 문구 그리드 다음에 바로 나오지 않고 사진 활용 팁 다음, 프롬프트 미리보기 제목 앞에 배치됐다.
- 자유 개별에 `우산을 쓰고 빗속에서 신나게 춤추는 모습`을 입력했을 때 GPT와 Gemini 프롬프트 첫 부분 및 선택 문구 배지에 반영됐다.
- 자유 입력 상태와 15종 개별 선택 상태를 분리해 15종에서 `고마워요`를 복사한 뒤 자유 개별로 돌아와도 입력 문구가 유지됐다.
- 15종 개별의 Gemini 원클릭 복사 완료 아이콘이 유지됐다.
- 390px 한글과 영문 화면에서 직접 입력 필드와 하단 복사 버튼 2개가 유지되고 가로 넘침이 없었다.
- 브라우저 콘솔 오류와 경고가 없고, 린트와 1,783개 모듈의 프로덕션 빌드가 경고 없이 통과했다.
- `15종 개별` 미리보기는 첫 문구를 고정 사용해 `1종`과 같은 내용처럼 보였고, 이 모드에서 GPT·Gemini 복사 버튼도 숨겨져 있었다.
- `15종 개별`의 15개 버튼은 복사를 즉시 실행하지 않고 미리보기 대상을 선택하며, 실제 복사는 공통 GPT·Gemini 버튼이 담당하도록 역할을 분리한다.
- `15종 개별`에서 5번 `고마워요`를 선택했을 때 GPT 미리보기가 기존 문구에서 `고마워요`로 바뀌었다.
- Gemini 미리보기에도 `고마워요`와 단일 이미지 레이아웃 조건이 반영됐고, Gemini 복사 버튼을 누르면 완료 아이콘이 표시됐다.
- 시트 전체, 자유 개별, 15종 개별 세 모드 모두 GPT와 Gemini 복사 버튼이 각각 1개씩 표시됐다.
- 390px 화면에서도 15개 문구 선택 버튼과 복사 버튼 2개가 유지되고 가로 넘침이 없었다.
- 브라우저 콘솔 오류와 경고가 없고, 린트와 1,783개 모듈의 프로덕션 빌드가 경고 없이 통과했다.
- 선택 전후 프롬프트 전체 값은 실제로 달랐지만 선택 문구가 맨 아래에만 있어 처음 보이는 240자는 동일했다.
- 사용자가 변경을 즉시 인지할 수 있도록 단일 이미지 프롬프트의 첫 부분과 미리보기 상단 배지에 선택 문구를 함께 표시한다.
- `고마워요` 선택 상태에서 GPT 프롬프트 첫 부분과 배지에 `고마워요`가 표시됐고, `사랑해요` 선택 후 두 위치가 즉시 갱신됐다.
- Gemini 프롬프트도 첫 180자 안에 `[Selected Phrase] "사랑해요"`가 표시됐다.
- 390px 화면에서 선택 문구 배지와 프롬프트 첫 부분이 일치했고 가로 넘침이 없었다.
- 영문 전환 시 현재 영문 문구 목록의 첫 값으로 안전하게 표시됐으며 브라우저 콘솔 오류와 경고가 없었다.
- `npm.cmd run lint`와 1,783개 모듈의 프로덕션 빌드가 경고 없이 통과했다.
- Gemini 품질 개선은 긴 지시문 추가보다 작업 분리를 우선한다. 먼저 문구 없는 기준 캐릭터 한 장을 만들고, 사용자가 채택한 결과 이미지를 다시 첨부해 15종을 개별 생성하도록 안내한다.
- 기준 캐릭터 단계는 현재 문구와 생성 모드에 영향을 받지 않는 독립 프롬프트를 만든다.
- 최종 개별 단계는 첨부 이미지를 새 디자인의 참고 자료가 아닌 변경 금지 기준으로 취급한다.
- 15종 시트는 표정과 구도 초안 기능으로 유지하며 최종 결과에는 15종 개별 분할을 권장한다.
- Gemini 탭의 기본 단계는 `기준 캐릭터 만들기`이며 문구와 무관한 정사각형 마스터 캐릭터 프롬프트를 표시한다.
- `기준 이미지로 생성` 단계는 첨부한 채택 이미지를 변경 금지 기준으로 명시하고, 시트 전체에는 초안 용도임을 함께 표시한다.
- 최종 단계에는 캐릭터 변화, 신체 잘림, 문구 오류를 최근 결과에서 필요한 부분만 고치는 후속 프롬프트 복사 버튼을 제공한다. 문구 오류 버튼은 문구 포함 설정일 때만 표시한다.
- `npm.cmd run lint`와 별도 출력 경로의 프로덕션 빌드가 통과했다. 브라우저에서 기준 단계, 최종 단계, 15종 개별 분할, 보정 버튼 완료 표시와 390px 가로 넘침 없음을 확인했으며 콘솔 오류와 경고는 없었다.
- 피사체 카테고리를 12개 전문 카테고리(동물, 새/조류, 어패류/해양생물, 곤충/벌레, 파충류/양서류, 공룡/고생물, 인형/팬시/문구, 인물, 판타지/사물, 로봇/SF, 디저트/음식, 식물/자연)로 분리하고 동물 태그에 섞여 있던 조류·해양생물을 각 전용 카테고리로 이전하여 카테고리 간 중복을 완전 제거했다.
- 신규 화풍 3종(B급 병맛/코믹 짤툰풍, 크레파스/오일파스텔 동화풍, 동양화/수묵담채화풍)을 추가하고 getExpandedArtStyleText 및 getSelectedArtStyle에 매핑했다.
- 신규 15종 문구 테마 8종 세트(새/반려조, 수족관/해양생물, 숲속/곤충, 파충류/우파루파, 공룡/고생물, 군인/곰신, K-직장인 속마음, 집사/고양이, 댕댕이/강아지)를 4개 국어(KO, EN, JA, ZH)로 완벽 동기화했다.
- 태그 단일/다중 선택 및 랜덤 조합 로직, getSelectedCharacterRoles를 24개 카테고리 키 기반으로 개편하여 안정성을 확보했다.
- `npm.cmd run build` 및 `npm.cmd run cap:build`가 오류 없이 성공적으로 완료됐다.
